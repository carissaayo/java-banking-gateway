services:
  mock-ledger:
    image: wiremock/wiremock:3.13.2
    ports:
      - "8081:8080"
    volumes:
      - ./local/wiremock/ledger:/home/wiremock
    command: ["--disable-banner"]

  mock-customer:
    image: wiremock/wiremock:3.13.2
    ports:
      - "8082:8080"
    volumes:
      - ./local/wiremock/customer:/home/wiremock
    command: ["--disable-banner"]
